apiVersion: v1
kind: Service
metadata:
  name: digital-arvsskifte-service
  namespace: digital-arvsskifte
  labels:
    app: digital-arvsskifte
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: digital-arvsskifte
    component: frontend